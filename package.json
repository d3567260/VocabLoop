{
  "name": "vocabloop",
  "version": "1.0.0",
  "private": true,
  "description": "VocabLoop - a spaced-repetition vocabulary learning app",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "start:server": "npm run start --workspace server",
    "test": "npm run test --workspace server && node --experimental-strip-types --test client/test/speech.test.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
