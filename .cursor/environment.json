{
  "name": "VocabLoop",
  "install": "npm install",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:server",
      "description": "VocabLoop Express API + SQLite backend on http://localhost:3001"
    },
    {
      "name": "web",
      "command": "npm run dev:client",
      "description": "VocabLoop Vite + React dev server on http://localhost:5173 (proxies /api to the backend)"
    }
  ],
  "ports": [
    { "name": "api", "port": 3001 },
    { "name": "web", "port": 5173 }
  ]
}
